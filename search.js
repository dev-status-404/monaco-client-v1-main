const fs=require('fs'),path=require('path');function walk(dir){for(const entry of fs.readdirSync(dir,{withFileTypes:true})){const p=path.join(dir,entry.name);if(entry.isDirectory()){walk(p);continue;}if(!entry.isFile())continue;if(!p.match(/\.(ts|tsx|js|jsx|json|mjs|cjs)$/))continue;const content=fs.readFileSync(p,'utf8');if(content.toLowerCase().includes('deposit'))console.log(p);}}walk('src');
